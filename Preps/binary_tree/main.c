#include <stdio.h>

// This is binary search tree, btw;

// To Do:
// Add / Insert
// Print
//   - height function
//   - size function
// Delete
// Search
// Rotations (left & right)
// Balancing the tree
//   - balance the tree - normal functuon
//   - balance the tree - when inserting / adding a new element
//   - check if balanced
// Check if trees / subtrees are identical


typedef struct node_t{
    int data;
    struct node_t *left, *right; // I'mma finally give up and put *[variable_name], instead of slapping it next to the variable type
} tree_t;

tree_t *add(tree_t *root, int data){
    
}

int main(){


    return 0;
}
