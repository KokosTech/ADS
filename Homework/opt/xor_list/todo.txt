Това домашно е пожелателно.

Като използвате кода от часа (или качения в сайта) добавете функции за:
проверяване дали дадена стойност се среща в списъка
добавяне и махане на елемент в края
добавяне и махане на елемент на произволен индекс
подреждане на елементите в списъка в нарастващ ред. Варианти с размяна само на стойностите и размяна на целите елементи
"срязване" на списъка на произволен индекс. Елементите в интервала [0, индекс) остават в оригиналния списък, а елементите от [индекс, край] се връщат като нов списък
промяна на функцията за добавяне така, че след всяко едно добавяне списъкът да е подреден в нарастващ ред и новият елемент е поставен на правилната позиция. Това как се отразява на бързодействието и сложността на функциите за добавяне, махане и търсене?