#include <stdio.h>
#include <stdlib.h>
#include "../../include/marto/graph.h"

char hamiltonian(struct graph_node_t *node, size_t size){
    if(size == 0){
        return 1;
    }
}

int main(){
    return 0;
}